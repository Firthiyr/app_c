from django.shortcuts import render


# Create your views here.
def add_to_cart(request, product_id): ...


def change_in_cart(request, product_id): ...


def delete_in_cart(request, product_id): ...
